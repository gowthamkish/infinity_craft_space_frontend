// CheckoutProgressBar is no longer used directly — Checkout.js uses MUI Stepper.
// Keeping this file to avoid import errors in case anything references it.
export const CheckoutProgressBar = () => null;
